[phases.setup]
nixPkgs = ["python311", "gcc", "g++"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p /app/chroma_db /app/data/medical_knowledge"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
